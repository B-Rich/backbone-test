// Generated by CoffeeScript 1.6.2
var TodoRouter, _ref,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

TodoRouter = (function(_super) {
  __extends(TodoRouter, _super);

  function TodoRouter() {
    _ref = TodoRouter.__super__.constructor.apply(this, arguments);
    return _ref;
  }

  TodoRouter.prototype.routes = {
    'todos/:id': 'show',
    '': 'index'
  };

  TodoRouter.prototype.start = function() {
    Backbone.history.start({
      pushState: true
    });
    return $(document).on('click', 'a:not([data-bypass])', function(evt) {
      var href, protocol;

      href = $(this).attr('href');
      protocol = this.protocol + '//';
      if (href.slice(protocol.length) !== protocol) {
        evt.preventDefault();
        return Backbone.history.navigate(href, {
          trigger: true
        });
      }
    });
  };

  TodoRouter.prototype.index = function() {
    var todoList;

    todoList = new TodoList;
    return todoList.fetch({
      success: function() {
        var newTodoView, todoListView;

        newTodoView = new NewTodoView({
          collection: todoList
        });
        todoListView = new TodoListView({
          collection: todoList
        });
        newTodoView.render();
        todoListView.render();
        $('#app').html(newTodoView.el);
        return $('#app').append(todoListView.el);
      }
    });
  };

  TodoRouter.prototype.show = function(url_id) {
    var todo;

    todo = new Todo({
      id: url_id
    });
    return todo.fetch({
      success: function() {
        var todoView;

        todoView = new TodoView({
          model: todo
        });
        todoView.render();
        return $('#app').html(todoView.el);
      }
    });
  };

  return TodoRouter;

})(Backbone.Router);
